package ch.vd.shared.tipi.engine.repository;

import ch.vd.shared.tipi.engine.model.TopProcessModel;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TopProcessModelRepository extends JpaRepository<TopProcessModel, Long> {
}
